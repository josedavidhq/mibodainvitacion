<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Valentina & Sebastián · 14 Junio 2025</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400;1,600&family=Cinzel:wght@400;600&family=Lato:wght@300;400&display=swap" rel="stylesheet"/>
<style>
  :root {
    --gold: #B8973A;
    --gold-light: #E2C97E;
    --gold-pale: #F5EDD0;
    --cream: #FDFAF4;
    --white: #FFFFFF;
    --ink: #2C2416;
    --ink-light: #6B5B3E;
    --border: rgba(184,151,58,0.3);
  }

  * { margin:0; padding:0; box-sizing:border-box; }

  html { scroll-behavior: smooth; }

  body {
    background: var(--cream);
    color: var(--ink);
    font-family: 'Lato', sans-serif;
    font-weight: 300;
    overflow-x: hidden;
  }

  /* ── SCROLL REVEAL ── */
  .reveal {
    opacity: 1;
    transform: none;
    transition: opacity 0.9s ease, transform 0.9s ease;
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }

  /* ── GOLD LINE ── */
  .gold-line {
    width: 60px; height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    margin: 0 auto;
  }
  .gold-line-wide {
    width: 160px; height: 1px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
    margin: 0 auto;
  }

  /* ── SECTION BASE ── */
  section {
    padding: 80px 24px;
    max-width: 680px;
    margin: 0 auto;
    text-align: center;
  }

  /* ═══════════════════════════════
     HERO
  ═══════════════════════════════ */
  #hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 60px 24px;
    position: relative;
    background:
      radial-gradient(ellipse 80% 60% at 50% 0%, rgba(229,209,143,0.18) 0%, transparent 70%),
      radial-gradient(ellipse 60% 40% at 50% 100%, rgba(229,209,143,0.12) 0%, transparent 70%),
      var(--cream);
    overflow: hidden;
  }

  /* Subtle corner ornaments */
  #hero::before, #hero::after {
    content: '';
    position: absolute;
    width: 180px; height: 180px;
    border: 1px solid var(--border);
    pointer-events: none;
  }
  #hero::before { top: 20px; left: 20px; border-right: none; border-bottom: none; }
  #hero::after  { bottom: 20px; right: 20px; border-left: none; border-top: none; }

  .hero-label {
    font-family: 'Cinzel', serif;
    font-size: 10px;
    letter-spacing: 4px;
    color: var(--gold);
    text-transform: uppercase;
    margin-bottom: 28px;
    animation: fadeUp 1s ease 0.2s both;
  }

  .hero-names {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(58px, 14vw, 96px);
    font-weight: 300;
    font-style: italic;
    line-height: 1.05;
    color: var(--ink);
    animation: fadeUp 1s ease 0.4s both;
  }

  .hero-names span { color: var(--gold); }

  .hero-amp {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(36px, 8vw, 56px);
    font-style: italic;
    color: var(--gold-light);
    display: block;
    line-height: 1;
    margin: -4px 0;
  }

  .hero-date {
    margin-top: 32px;
    font-family: 'Cinzel', serif;
    font-size: 13px;
    letter-spacing: 3px;
    color: var(--ink-light);
    animation: fadeUp 1s ease 0.8s both;
  }

  .hero-scroll {
    position: absolute;
    bottom: 36px;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    animation: fadeUp 1s ease 1.4s both;
  }
  .hero-scroll span {
    font-family: 'Cinzel', serif;
    font-size: 9px;
    letter-spacing: 3px;
    color: var(--gold);
  }
  .scroll-line {
    width: 1px; height: 40px;
    background: linear-gradient(to bottom, var(--gold), transparent);
    animation: scrollPulse 2s ease-in-out infinite;
  }

  @keyframes scrollPulse {
    0%, 100% { opacity: 0.4; transform: scaleY(1); }
    50% { opacity: 1; transform: scaleY(1.15); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  /* ═══════════════════════════════
     ORNAMENT DIVIDER
  ═══════════════════════════════ */
  .ornament {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    margin: 28px auto;
  }
  .ornament .line { flex: 1; max-width: 80px; height: 1px; background: linear-gradient(90deg, transparent, var(--gold)); }
  .ornament .line.r { background: linear-gradient(90deg, var(--gold), transparent); }
  .ornament .diamond {
    width: 7px; height: 7px;
    background: var(--gold);
    transform: rotate(45deg);
  }
  .ornament .dot {
    width: 4px; height: 4px;
    background: var(--gold-light);
    border-radius: 50%;
  }

  /* ═══════════════════════════════
     SECTION TITLES
  ═══════════════════════════════ */
  .section-tag {
    font-family: 'Cinzel', serif;
    font-size: 9px;
    letter-spacing: 4px;
    color: var(--gold);
    text-transform: uppercase;
    margin-bottom: 16px;
    display: block;
  }
  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(32px, 7vw, 48px);
    font-weight: 300;
    font-style: italic;
    color: var(--ink);
    line-height: 1.2;
    margin-bottom: 12px;
  }
  .section-body {
    font-size: 14px;
    line-height: 1.9;
    color: var(--ink-light);
    max-width: 480px;
    margin: 0 auto;
  }

  /* ═══════════════════════════════
     INTRO / STORY
  ═══════════════════════════════ */
  #intro {
    background: var(--white);
    max-width: 100%;
    padding: 80px 24px;
  }
  #intro .inner { max-width: 600px; margin: 0 auto; text-align: center; }

  .quote-mark {
    font-family: 'Cormorant Garamond', serif;
    font-size: 80px;
    line-height: 0.5;
    color: var(--gold-pale);
    display: block;
    margin-bottom: 16px;
  }

  /* ═══════════════════════════════
     COUNTDOWN
  ═══════════════════════════════ */
  #countdown {
    background:
      linear-gradient(135deg, rgba(184,151,58,0.06) 0%, transparent 60%),
      var(--cream);
  }

  .countdown-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
    margin-top: 40px;
  }
  .countdown-item {
    background: var(--white);
    border: 1px solid var(--border);
    padding: 24px 8px 18px;
    position: relative;
  }
  .countdown-item::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }
  .countdown-num {
    font-family: 'Cormorant Garamond', serif;
    font-size: 48px;
    font-weight: 300;
    color: var(--ink);
    line-height: 1;
    display: block;
  }
  .countdown-label {
    font-family: 'Cinzel', serif;
    font-size: 8px;
    letter-spacing: 3px;
    color: var(--gold);
    text-transform: uppercase;
    display: block;
    margin-top: 6px;
  }

  /* ═══════════════════════════════
     EVENT DETAILS
  ═══════════════════════════════ */
  #details { background: var(--white); max-width: 100%; padding: 80px 24px; }
  #details .inner { max-width: 680px; margin: 0 auto; text-align: center; }

  .details-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-top: 40px;
  }
  @media(max-width:500px){ .details-cards { grid-template-columns: 1fr; } }

  .detail-card {
    border: 1px solid var(--border);
    padding: 36px 24px;
    position: relative;
    background: var(--cream);
    text-align: center;
  }
  .detail-card::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }
  .detail-icon {
    font-size: 28px;
    display: block;
    margin-bottom: 16px;
  }
  .detail-title {
    font-family: 'Cinzel', serif;
    font-size: 10px;
    letter-spacing: 3px;
    color: var(--gold);
    text-transform: uppercase;
    margin-bottom: 12px;
  }
  .detail-main {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px;
    font-style: italic;
    color: var(--ink);
    margin-bottom: 6px;
  }
  .detail-sub {
    font-size: 12px;
    color: var(--ink-light);
    line-height: 1.7;
  }

  /* ═══════════════════════════════
     GALLERY
  ═══════════════════════════════ */
  #gallery { background: var(--cream); }

  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: 180px 180px;
    gap: 10px;
    margin-top: 40px;
  }
  @media(max-width:500px){ .gallery-grid { grid-template-columns: repeat(2,1fr); grid-template-rows: repeat(3,140px); } }

  .gallery-item {
    overflow: hidden;
    position: relative;
    background: var(--gold-pale);
    border: 1px solid var(--border);
  }
  .gallery-item:first-child { grid-row: span 2; }
  .gallery-placeholder {
    width: 100%; height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 8px;
    background: linear-gradient(135deg, #f7f0e0, #ede3c8);
    color: var(--gold);
    font-family: 'Cormorant Garamond', serif;
    font-style: italic;
    font-size: 13px;
    transition: transform 0.5s ease;
  }
  .gallery-item:hover .gallery-placeholder { transform: scale(1.04); }
  .gallery-placeholder span { font-size: 28px; opacity: 0.6; }
  .gallery-note {
    margin-top: 20px;
    font-size: 12px;
    color: var(--ink-light);
    font-style: italic;
  }

  /* ═══════════════════════════════
     MAP
  ═══════════════════════════════ */
  #map-section { background: var(--white); max-width:100%; padding: 80px 24px; }
  #map-section .inner { max-width: 680px; margin: 0 auto; text-align:center; }

  .location-card {
    margin-top: 36px;
    border: 1px solid var(--border);
    background: var(--cream);
    padding: 48px 32px;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }
  .location-card::before {
    content: '';
    position: absolute; top:0; left:0; right:0; height:2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }
  .location-card::after {
    content: '';
    position: absolute; bottom:0; left:0; right:0; height:2px;
    background: linear-gradient(90deg, transparent, var(--gold), transparent);
  }
  .location-icon {
    font-size: 36px;
    line-height: 1;
  }
  .location-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 26px;
    font-style: italic;
    color: var(--ink);
  }
  .location-address {
    font-size: 14px;
    color: var(--ink-light);
    line-height: 1.9;
    text-align: center;
  }
  .location-btn {
    display: inline-block;
    margin-top: 4px;
    padding: 12px 32px;
    border: 1px solid var(--gold);
    font-family: 'Cinzel', serif;
    font-size: 9px;
    letter-spacing: 3px;
    color: var(--gold);
    text-decoration: none;
    text-transform: uppercase;
    transition: background 0.3s, color 0.3s;
  }
  .location-btn:hover { background: var(--gold); color: var(--white); }

  .location-card iframe {
    border: 1px solid var(--border);
    border-radius: 8px;
  }

  /* ── MUSIC BUTTON ── */
  #music-btn {
    position: fixed;
    bottom: 28px;
    right: 28px;
    z-index: 200;
    width: 52px; height: 52px;
    background: var(--gold);
    border: none;
    border-radius: 50%;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 4px 20px rgba(184,151,58,0.45);
    transition: transform 0.2s, background 0.3s;
  }
  #music-btn:hover { transform: scale(1.1); background: var(--ink); }
  #music-btn svg { width:22px; height:22px; fill: white; }
  .music-ripple {
    position: absolute;
    width: 52px; height: 52px;
    border-radius: 50%;
    border: 1px solid var(--gold);
    animation: ripple 2s ease-out infinite;
    pointer-events: none;
  }
  @keyframes ripple {
    0%   { transform: scale(1); opacity: 0.6; }
    100% { transform: scale(2.2); opacity: 0; }
  }

  /* ═══════════════════════════════
     QR GIFTS
  ═══════════════════════════════ */
  #gifts { background: var(--cream); }

  .qr-card {
    margin-top: 40px;
    border: 1px solid var(--border);
    background: var(--white);
    padding: 48px 32px;
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 24px;
  }
  .qr-card::before, .qr-card::after {
    content: '';
    position: absolute;
    width: 24px; height: 24px;
    border-color: var(--gold);
    border-style: solid;
  }
  .qr-card::before { top:10px; left:10px; border-width: 1px 0 0 1px; }
  .qr-card::after  { bottom:10px; right:10px; border-width: 0 1px 1px 0; }

  .qr-box {
    width: 220px;
    height: 220px;
    padding: 16px;
    background: var(--white);
    border: 1px solid var(--border);
    display: flex;
    justify-content: center;
    align-items: center;
    }
  .qr-box svg { width:100%; height:100%; }

  .qr-label {
    font-family: 'Cormorant Garamond', serif;
    font-size: 20px;
    font-style: italic;
    color: var(--ink);
  }
  .qr-sub { font-size: 13px; color: var(--ink-light); text-align: center; max-width: 340px; line-height: 1.8; }

  .wish-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
    margin-top: 8px;
  }
  .wish-tag {
    border: 1px solid var(--border);
    padding: 8px 18px;
    font-size: 12px;
    color: var(--ink-light);
    font-family: 'Cinzel', serif;
    letter-spacing: 1px;
    background: var(--cream);
  }

  /* ═══════════════════════════════
     RSVP
  ═══════════════════════════════ */
  #rsvp { background: var(--white); max-width:100%; padding: 80px 24px 100px; }
  #rsvp .inner { max-width: 560px; margin: 0 auto; text-align:center; }

  .rsvp-form {
    margin-top: 40px;
    display: flex;
    flex-direction: column;
    gap: 16px;
    text-align: left;
  }

  .form-group { display: flex; flex-direction: column; gap: 6px; }

  .form-group label {
    font-family: 'Cinzel', serif;
    font-size: 9px;
    letter-spacing: 3px;
    color: var(--gold);
    text-transform: uppercase;
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    border: 1px solid var(--border);
    background: var(--cream);
    padding: 14px 16px;
    font-family: 'Lato', sans-serif;
    font-size: 14px;
    font-weight: 300;
    color: var(--ink);
    outline: none;
    transition: border-color 0.3s, background 0.3s;
    -webkit-appearance: none;
    border-radius: 0;
    width: 100%;
  }
  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    border-color: var(--gold);
    background: var(--white);
  }
  .form-group textarea { resize: vertical; min-height: 90px; }

  .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  @media(max-width:480px){ .form-row { grid-template-columns: 1fr; } }

  .rsvp-btn {
    margin-top: 8px;
    padding: 18px 48px;
    background: var(--gold);
    color: var(--white);
    font-family: 'Cinzel', serif;
    font-size: 11px;
    letter-spacing: 4px;
    text-transform: uppercase;
    border: none;
    cursor: pointer;
    transition: background 0.3s, transform 0.2s;
    align-self: center;
    width: 100%;
  }
  .rsvp-btn:hover { background: var(--ink); transform: translateY(-1px); }

  .rsvp-success {
    display: none;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    padding: 48px 24px;
    border: 1px solid var(--border);
    background: var(--cream);
  }
  .rsvp-success .check { font-size: 40px; }
  .rsvp-success p { font-family:'Cormorant Garamond',serif; font-size:22px; font-style:italic; color:var(--ink); }
  .rsvp-success small { color:var(--ink-light); font-size:13px; }

  /* ═══════════════════════════════
     FOOTER
  ═══════════════════════════════ */
  footer {
    background: var(--ink);
    padding: 60px 24px;
    text-align: center;
  }
  footer .f-names {
    font-family: 'Cormorant Garamond', serif;
    font-size: 32px;
    font-style: italic;
    font-weight: 300;
    color: var(--gold-light);
    margin-bottom: 12px;
  }
  footer .f-date {
    font-family: 'Cinzel', serif;
    font-size: 10px;
    letter-spacing: 4px;
    color: rgba(255,255,255,0.4);
    margin-bottom: 24px;
  }
  footer .f-verse {
    font-style: italic;
    font-size: 13px;
    color: rgba(255,255,255,0.35);
    max-width: 360px;
    margin: 0 auto;
    line-height: 1.8;
  }

  /* ── NAV ── */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    padding: 16px 32px;
    display: flex;
    justify-content: center;
    gap: 32px;
    background: rgba(253,250,244,0.9);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    transition: opacity 0.4s;
  }
  nav a {
    font-family: 'Cinzel', serif;
    font-size: 9px;
    letter-spacing: 3px;
    color: var(--ink-light);
    text-decoration: none;
    text-transform: uppercase;
    transition: color 0.3s;
  }
  nav a:hover { color: var(--gold); }

  /* Simple QR SVG generation */
  .qr-placeholder {
    width: 136px; height: 136px;
    background: repeating-conic-gradient(#000 0% 25%, #fff 0% 50%) 0 0 / 8px 8px;
    opacity: 0.15;
    position: relative;
  }


  

</style>
</head>
<body>

  <audio id="music" loop>
  <source src="musica.mp3" type="audio/mpeg">
</audio>

<!-- NAV -->
<nav>
  <a href="#countdown">Fecha</a>
  <a href="#details">Evento</a>
  <a href="#gallery">Fotos</a>
  <a href="#map-section">Lugar</a>
  <a href="#gifts">Regalos</a>
  
</nav>

<!-- ═══ HERO ═══ -->
<section id="hero">
  <p class="hero-label">Los invitamos a celebrar</p>
  <div class="hero-names">
    <span style="display:block">Valentina</span>
    <span class="hero-amp">&amp;</span>
    <span style="display:block">Sebastián</span>
  </div>
  <div class="ornament" style="margin-top:28px;animation:fadeUp 1s ease 0.9s both">
    <div class="line"></div>
    <div class="dot"></div>
    <div class="diamond"></div>
    <div class="dot"></div>
    <div class="line r"></div>
  </div>
  <p class="hero-date">14 · JUNIO · 2025 · LIMA, PERÚ</p>
  <div class="hero-scroll">
    <span>SCROLL</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- ═══ INTRO ═══ -->
<section id="intro">
  <div class="inner reveal">
    <span class="quote-mark">"</span>
    <p class="section-body" style="font-size:16px; font-family:'Cormorant Garamond',serif; font-style:italic; color:var(--ink); line-height:1.9; font-size:18px;">
      Dos corazones que se encontraron en el camino<br>y eligieron recorrerlo juntos para siempre.
    </p>
    <div class="ornament" style="margin-top:32px">
      <div class="line"></div>
      <div class="dot"></div>
      <div class="diamond"></div>
      <div class="dot"></div>
      <div class="line r"></div>
    </div>
    <p class="section-body" style="margin-top:20px;">
      Con la bendición de nuestras familias,<br>
      tenemos el honor de invitarlos a compartir<br>
      este momento tan especial en nuestras vidas.
    </p>
  </div>
</section>

<!-- ═══ COUNTDOWN ═══ -->
<section id="countdown">
  <div class="reveal">
    <span class="section-tag">El gran día llega en</span>
    <h2 class="section-title">Cuenta Regresiva</h2>
    <div class="gold-line"></div>
    <div class="countdown-grid" id="cdGrid">
      <div class="countdown-item"><span class="countdown-num" id="cd-days">00</span><span class="countdown-label">Días</span></div>
      <div class="countdown-item"><span class="countdown-num" id="cd-hours">00</span><span class="countdown-label">Horas</span></div>
      <div class="countdown-item"><span class="countdown-num" id="cd-min">00</span><span class="countdown-label">Minutos</span></div>
      <div class="countdown-item"><span class="countdown-num" id="cd-sec">00</span><span class="countdown-label">Segundos</span></div>
    </div>
  </div>
</section>

<!-- ═══ DETAILS ═══ -->
<section id="details" style="background:var(--white);max-width:100%;padding:80px 24px;">
  <div class="inner reveal">
    <span class="section-tag">Información del evento</span>
    <h2 class="section-title">El Día Especial</h2>
    <div class="gold-line"></div>
    <div class="details-cards" style="margin-top:40px;">
      <div class="detail-card reveal">
        <span class="detail-icon">💒</span>
        <p class="detail-title">Ceremonia</p>
        <p class="detail-main">Iglesia San Francisco</p>
        <p class="detail-sub">Sábado 14 de junio, 2025<br>5:00 p.m.<br>Jirón Ancash 471, Lima</p>
      </div>
      <div class="detail-card reveal">
        <span class="detail-icon">🥂</span>
        <p class="detail-title">Recepción</p>
        <p class="detail-main">Hacienda San Martín</p>
        <p class="detail-sub">A partir de las 7:30 p.m.<br>Av. Los Rosales 248<br>San Isidro, Lima</p>
      </div>
      <div class="detail-card reveal">
        <span class="detail-icon">👔</span>
        <p class="detail-title">Dress Code</p>
        <p class="detail-main">Formal Elegante</p>
        <p class="detail-sub">Tonos claros bienvenidos<br>Sin vestidos blancos<br>Tacones con protector</p>
      </div>
      <div class="detail-card reveal">
        <span class="detail-icon">🕐</span>
        <p class="detail-title">Importante</p>
        <p class="detail-main">Puntualidad</p>
        <p class="detail-sub">Les pedimos llegar<br>15 minutos antes<br>de la ceremonia</p>
      </div>
    </div>
  </div>
</section>

<!-- ═══ GALLERY ═══ -->
<section id="gallery">
  <div class="reveal">
    <span class="section-tag">Nuestra historia</span>
    <h2 class="section-title">Momentos Juntos</h2>
    <div class="gold-line"></div>
    <div class="gallery-grid">
      <div class="gallery-item">
        <div class="gallery-placeholder">
          <span>✦</span>
          Foto 1
        </div>
      </div>
      <div class="gallery-item">
        <div class="gallery-placeholder">
          <span>✦</span>
          Foto 2
        </div>
      </div>
      <div class="gallery-item">
        <div class="gallery-placeholder">
          <span>✦</span>
          Foto 3
        </div>
      </div>
      <div class="gallery-item">
        <div class="gallery-placeholder">
          <span>✦</span>
          Foto 4
        </div>
      </div>
      <div class="gallery-item">
        <div class="gallery-placeholder">
          <span>✦</span>
          Foto 5
        </div>
      </div>
    </div>
    <p class="gallery-note">* Aquí irán las fotos de los novios</p>
  </div>
</section>

<!-- ═══ LOCATION ═══ -->
<section id="map-section" style="background:var(--white);max-width:100%;padding:80px 24px;">
  <div class="inner reveal">
    <span class="section-tag">¿Cómo llegar?</span>
    <h2 class="section-title">Ubicación</h2>
    <div class="gold-line"></div>

    <div class="details-cards" style="margin-top:40px;">


      <div class="location-card reveal">
        <span class="location-icon">💒</span>
        <p class="detail-title">Ceremonia</p>
        <p class="location-name">Iglesia San Francisco</p>
        <p class="location-address">Jirón Ancash 471<br>Cercado de Lima, Lima</p>

        <a class="location-btn" href="https://maps.google.com/?q=Iglesia+San+Francisco+Lima" target="_blank">
          Ver en Google Maps ↗
        </a>

        <!-- 👇 AQUÍ VA EL MAPA -->
        <iframe 
          src="https://www.google.com/maps?q=Iglesia+San+Francisco+Lima&output=embed"
          width="100%" 
          height="220" 
          style="border:0; border-radius:8px; margin-top:12px;"
          loading="lazy">
        </iframe>
      </div>
      
      <div class="location-card reveal">
        <span class="location-icon">🥂</span>
        <p class="detail-title">Recepción</p>
        <p class="location-name">Hacienda San Martín</p>
        <p class="location-address">
          Av. Los Rosales 248<br>
          San Isidro, Lima<br><br>
          Estacionamiento disponible<br>
          Valet parking incluido
        </p>

        <a class="location-btn" href="https://maps.google.com/?q=San+Isidro+Lima" target="_blank">
          Ver en Google Maps ↗
        </a>

        <!-- 👇 AQUÍ TAMBIÉN -->
        <iframe 
          src="https://www.google.com/maps?q=San+Isidro+Lima&output=embed"
          width="100%" 
          height="220" 
          style="border:0; border-radius:8px; margin-top:12px;"
          loading="lazy">
        </iframe>
      </div>


    </div>
  </div>
</section>

<!-- ═══ GIFTS / QR ═══ -->
<section id="gifts">
  <div class="reveal">
    <span class="section-tag">Lista de regalos</span>
    <h2 class="section-title">Mesa de Regalos</h2>
    <div class="gold-line"></div>
    <p class="section-body" style="margin-top:20px;">
      Su presencia es nuestro mayor regalo. Si desean obsequiarnos algo,
      aquí encontrarán nuestra lista de regalos sugeridos.
    </p>

    <div class="qr-card reveal">

        <!-- QR -->
        <div class="qr-box">
            <img 
            src="https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=https://es-d-8993569620260327-019d1be6-7452-7d03-97c2-e411b910d929.codepen.dev/" 
            alt="QR Lista de Regalos"
            >
        </div>

        <!-- TEXTO -->
        <p class="qr-label">Escanea para ver la lista</p>

        <p class="qr-sub">
            Hemos preparado una selección de regalos para facilitar tu elección. 
            Cualquier detalle con amor es bienvenido.
        </p>

        <!-- BOTÓN (ahora bien ubicado) -->
        <a href="lista.html" 
            target="_blank" 
            class="btn-regalo">
            Ver lista de regalos
        </a>

        <div class="wish-tags">
            <div class="wish-tag">🏠 Hogar</div>
            <div class="wish-tag">✈️ Luna de Miel</div>
            <div class="wish-tag">🍳 Cocina</div>
            <div class="wish-tag">🎁 Sorpresa</div>
        </div>

        <p class="qr-footer">
            Escanea o haz clic para acceder a nuestra lista de regalos
        </p>

        </div>



  </div>
</section>



<!-- ═══ RSVP ═══ -->


<!-- ═══ FOOTER ═══ -->
<footer>
  <div class="ornament" style="margin-bottom:28px;">
    <div class="line" style="background:linear-gradient(90deg,transparent,rgba(184,151,58,0.5));"></div>
    <div class="diamond" style="background:var(--gold-light);"></div>
    <div class="line r" style="background:linear-gradient(90deg,rgba(184,151,58,0.5),transparent);"></div>
  </div>
  <p class="f-names">Valentina &amp; Sebastián</p>
  <p class="f-date">14 · JUNIO · 2025 · LIMA</p>
  <p class="f-verse">"El amor es paciente, es bondadoso... todo lo sufre, todo lo cree, todo lo espera, todo lo soporta." — 1 Corintios 13:4</p>
</footer>

<script>
document.addEventListener("DOMContentLoaded", function() {

  // ── COUNTDOWN ──
  function updateCountdown() {
    
    const wedding = new Date('2026-03-31T17:00:00');
    const now = new Date();
    const diff = wedding - now;
    if (diff <= 0) return;

    const d = Math.floor(diff / 86400000);
    const h = Math.floor((diff % 86400000) / 3600000);
    const m = Math.floor((diff % 3600000) / 60000);
    const s = Math.floor((diff % 60000) / 1000);

    document.getElementById('cd-days').textContent  = String(d).padStart(2,'0');
    document.getElementById('cd-hours').textContent = String(h).padStart(2,'0');
    document.getElementById('cd-min').textContent   = String(m).padStart(2,'0');
    document.getElementById('cd-sec').textContent   = String(s).padStart(2,'0');
  }

  updateCountdown();
  setInterval(updateCountdown, 1000);

  // ── SCROLL REVEAL ──
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(e => {
      if (e.isIntersecting) {
        e.target.classList.add('visible');
      }
    });
  });

  document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

  // ── RSVP ──
  const form = document.getElementById('rsvpForm');
  if (form) {
    form.addEventListener('submit', function(e) {
      e.preventDefault();
      this.style.display = 'none';
      document.getElementById('rsvpSuccess').style.display = 'flex';
    });
  }

});



// ── MUSIC ──
document.addEventListener("DOMContentLoaded", function() {

  // ... todo tu código anterior ...

  // ── MUSIC ──
  const music = document.getElementById("music");
  const btn = document.getElementById("music-btn");

  let playing = false;

  btn.addEventListener("click", () => {
    if (!playing) {
      music.play();
      btn.innerHTML = "⏸";
    } else {
      music.pause();
      btn.innerHTML = "▶";
    }
    playing = !playing;
  });

});

  
</script>

  <button id="music-btn">
  ▶
</button>
</body>
</html>
